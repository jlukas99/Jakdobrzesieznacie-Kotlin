<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                                   xmlns:app="http://schemas.android.com/apk/res-auto"
                                                   xmlns:tools="http://schemas.android.com/tools"
                                                   android:layout_width="wrap_content"
                                                   android:layout_height="wrap_content"
                                                   android:layout_marginBottom="4dp" android:layout_marginTop="4dp"
                                                   android:id="@+id/setLayout" android:layout_marginEnd="4dp"
                                                   android:layout_marginStart="4dp">


    <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:id="@+id/question_number_background"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" app:srcCompat="@drawable/input_overlay_white"/>
    <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content" android:id="@+id/question_number"
            android:maxLines="1" android:singleLine="true"
            android:text="0"
            android:textAlignment="center"
            app:layout_constraintTop_toTopOf="@+id/question_number_background"
            app:layout_constraintEnd_toEndOf="@+id/question_number_background"
            app:layout_constraintStart_toStartOf="@+id/question_number_background"
            app:layout_constraintBottom_toBottomOf="@+id/question_number_background"
            android:textColor="@color/colorWhite"/>

</androidx.constraintlayout.widget.ConstraintLayout>